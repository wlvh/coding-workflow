interface:
  display_name: "工作流文档同步"
  short_description: "默认同步当前 Git 根，按请求语言选模板，按明确意图创建 draft PR"
  default_prompt: "使用 $workflow-docs-sync 同步当前 Git 根的工作流文档；按当前请求语言选择 zh 或 en，只有用户明确要求时才创建 draft PR。"

policy:
  allow_implicit_invocation: false
